# Elastic Cloud APM Configuration
ELASTIC_APM_SERVER_URL=https://your-cloud-id.apm.us-west-2.aws.cloud.es.io:443
ELASTIC_APM_SECRET_TOKEN=your-secret-token-here

# Elastic Agent Configuration (Choose one method)
# Method 1: Fleet-managed (Recommended)
FLEET_URL=https://your-fleet-server-url.elastic.cloud:443
FLEET_ENROLLMENT_TOKEN=your-fleet-enrollment-token

# Method 2: Standalone (if not using Fleet)
# ELASTIC_CLOUD_ID=your-cloud-id
# ELASTIC_CLOUD_AUTH=elastic:your-cloud-password

# Optional: If you want to set these
ELASTIC_APM_ENVIRONMENT=development
ELASTIC_APM_SERVICE_NAME=hotel-pms-backend